{
  "name": "foyir-mono",
  "private": true,
  "engines": {
    "node": "14.18.1"
  },
  "scripts": {
    "build": "npx lerna run build",
    "start": "npx lerna run start:prod --stream",
    "start:dev": "npx lerna run --parallel start:dev",
    "deploy:dev": "eb deploy foyir-mono-development",
    "prepublish": "npm run build",
    "test": "lerna run test && lerna run test:e2e"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "express-basic-auth": "^1.2.1"
  }
}
