{
  "name": "foyir-mono",
  "private": true,
  "engines": {
    "node": "14.18.1"
  },
  "scripts": {
    "build": "npx lerna run build",
    "deploy:dev": "npm run build && eb deploy foyir-mono-development",
    "start": "npm run build && npx lerna run start:prod --stream",
    "start:dev": "npx lerna run --parallel start:dev",
    "test": "lerna run test && lerna run test:e2e"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "express-basic-auth": "^1.2.1"
  }
}
