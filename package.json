{
  "name": "foyir-mono",
  "private": true,
  "engines": {
    "node": "14.18.1"
  },
  "scripts": {
    "clean": "lerna clean",
    "install": "lerna bootstrap",
    "build": "lerna run build",
    "start": "npm run clean && npm run install && npm run build && npm run start:dev",
    "start:dev": "lerna run --parallel start:dev",
    "start:prod": "lerna run start:prod",
    "deploy": "eb deploy"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "lerna": "^4.0.0"
  }
}
